name: Build & Push DAS Admin
on:
  push:
    branches:
      - main
      - "**staging**"
  workflow_dispatch:
jobs:
  build-and-push:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Compute tags
        id: tags
        run: |
          SHA_SHORT=$(echo "${{ github.sha }}" | cut -c1-7)
          if [[ "${{ github.ref_name }}" == "main" ]]; then
            echo "tags=nejishow/das-admin:latest,nejishow/das-admin:$SHA_SHORT" >> $GITHUB_OUTPUT
          else
            echo "tags=nejishow/das-admin:staging,nejishow/das-admin:staging-$SHA_SHORT" >> $GITHUB_OUTPUT
          fi
      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3
      - name: Login DockerHub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}
      - name: Build & Push
        uses: docker/build-push-action@v5
        with:
          context: .
          push: true
          tags: ${{ steps.tags.outputs.tags }}
          cache-from: type=gha
          cache-to: type=gha,mode=max

      # Déclenche le déploiement Jenkins — uniquement sur main
      # - name: Trigger Jenkins deploy
      #   if: github.ref_name == 'main'
      #   run: |
      #     curl -fsSL -X POST \
      #       "${{ secrets.JENKINS_URL }}/job/dasAdmin/buildWithParameters" \
      #       --user "${{ secrets.JENKINS_USER }}:${{ secrets.JENKINS_TOKEN }}" \
      #       --data-urlencode "token=${{ secrets.JENKINS_TRIGGER_TOKEN }}" \
      #       --data-urlencode "SERVICE=das-admin"
